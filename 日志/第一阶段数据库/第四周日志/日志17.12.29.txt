今日的进度：完成几个组成breed的集合类（组件类）
依照当前进度和设计构想：

0 护甲类型（决定了其属性和战斗时对不同情况的互动，例如金属怕电，木制怕火等），
0 部队类型（决定了其使用的是否使用特定组件和属性），
0（待手动） 技能类型（与部队类型有关，在部队类型中可以定义），
0 动作模组（不同状态的种种动画，以固定某Animation名字形式存储），
1 物理实体（包含质量，速度限制等属性），
0 武器种类（发射和攻击等操作的实现在武器类中，可以用于解耦），
0（待手动）事件监听类型（对种种事件的响应工作），
0（待手动）使用UI模组（与特定效果表示和语言动作有关），
1 使用控制方案（即Controller类的名字或id，用于在创建敌人时添加Controller）
本身属性：攻击力，攻击速度，防御力，行进速度，生命值，装填能力（如果是远程）

今日需完成
Enemy类（继承自Entity）
护甲类型，部队类型，动作模组，武器种类
开始Enemy类时发现addController有问题，其实是没有写继承，其实不需要对设计太多修改
编程实践比空想重要！

WeaponList类现已实现。

加入Weapon类，一些简单的攻击方法和判定尚未实现，但是现在可以加入Entity并且同时移动了，
*****************
以上为28日日志
*****************
今天目标，动作模组，护甲类型，部队类型（含技能）。

护甲类型现已实现，问题有
1：localZOrder应该有方向性，但现在只是笼统地设为+1
2：position的设置不合理，需要一个持有数据也许

基本实现了Unit该有的功能，但是问题在于Skill很笼统，并没有完全实现，也许还需要一个手撸的skill表和一些switch语句来完成拓展，
目前而言skill只作为一个测试类出现，很简陋

进一步的考虑：动作模组应该是静止时或对外界反应是的自身动画，而攻击之类的动画应该在其所属的部分实现。 