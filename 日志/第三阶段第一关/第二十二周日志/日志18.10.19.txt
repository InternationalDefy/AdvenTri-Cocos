现在主菜单的效果并不如人意，问题集中表现在以下几个方面：
1：随机运动的时间应该是一个不同的值，不能使用定值
2：随机运动的物体颜色应该更暗一些
3：铅笔动画仍需调整。
4：单元件素材仍未完成。
接受素材·中仅有翻页动画可用。。。
5：主菜单的翻页动画？

1解决
2解决
3解决 但是铅笔画风还待考虑。

先不考虑45，先进行第一场景的创建和测试体系的搭建。

测试体系需要：
A 完全数据驱动的大关，
B 预加载每一个系统并且监控其加载情况
C 可以用一个特定的存档，打开特定的场景
D 存档编辑能力

*******************
以上为9月24日日志
*******************
最近两天很慌，也没什么卵用，现在回归状态，今天的第一个任务，完成Controller部分的整理。

一眼看出来的类型和凭借回忆得到的结论不能作为文档书写的依据。要勤考察。
以每个类的具体属性和行事方法为依据，不能形而上学。

整理时发现巨大问题：
Controller体系实现太过失败：简单问题复杂化。
且有实体状态耦合，这里考虑将实体状态简化以达到目的。

Controller系统的记录完成，但是这个类写得很不好，我们必须承认它。

下面开始对事件监听系统的记录。

没心情做了，明天吧。

*******************
以上为18日日志
*******************
整理时发现巨大问题：
Controller体系实现太过失败：简单问题复杂化。
且有实体状态耦合，这里考虑将实体状态简化以达到目的。

今天的第一个任务，完成事件体系的文档记录。
Event体系算是写得比较完善的一个系统，需要注意的改进有两点：
1：添加方法没必要写三个之多，可以直接在Node层面增加EventParser的处理。
2：Parser可以使用string-map的形式来避免过多的if 语句。
3：两函数activeParser和setParent功能有耦合。

Node-Entity-UIItem对EventParser的处理不同：
EntityUIItem各自维护一个自己的监听表，Node否，Entity作为Parser的父节点存在，其余否。
UIItem的Parsers只有在Active的时候才会setParent
现在可以清楚Event运行机制了。

现在清楚不代表一直清楚，赶紧写文档。

物理掩码是重要的一环记录在 系统记录-Event 中，实现于 PhysicsParser 类中 在必要时修改和设计。

数据库格式记录完成，拓展方法有待实现，先吃饭。

困。。。昨晚拓展方法-Event记录之后先休息一会。

填完/

完成了Animation部分的记录，似乎记录可以简洁一些，记录 最重要的 和 最需要扩展的 内容即可！