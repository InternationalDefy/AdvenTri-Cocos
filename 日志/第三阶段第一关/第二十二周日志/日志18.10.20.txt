现在主菜单的效果并不如人意，问题集中表现在以下几个方面：
1：随机运动的时间应该是一个不同的值，不能使用定值
2：随机运动的物体颜色应该更暗一些
3：铅笔动画仍需调整。
4：单元件素材仍未完成。
接受素材·中仅有翻页动画可用。。。
5：主菜单的翻页动画？

1解决
2解决
3解决 但是铅笔画风还待考虑。

先不考虑45，先进行第一场景的创建和测试体系的搭建。

测试体系需要：
A 完全数据驱动的大关，
B 预加载每一个系统并且监控其加载情况
C 可以用一个特定的存档，打开特定的场景
D 存档编辑能力

*******************
以上为9月24日日志
*******************
今天的第一个任务，完成事件体系的文档记录。
Event体系算是写得比较完善的一个系统，需要注意的改进有两点：
1：添加方法没必要写三个之多，可以直接在Node层面增加EventParser的处理。
2：Parser可以使用string-map的形式来避免过多的if 语句。
3：两函数activeParser和setParent功能有耦合。

Node-Entity-UIItem对EventParser的处理不同：
EntityUIItem各自维护一个自己的监听表，Node否，Entity作为Parser的父节点存在，其余否。
UIItem的Parsers只有在Active的时候才会setParent
现在可以清楚Event运行机制了。

现在清楚不代表一直清楚，赶紧写文档。

物理掩码是重要的一环记录在 系统记录-Event 中，实现于 PhysicsParser 类中 在必要时修改和设计。

数据库格式记录完成，拓展方法有待实现，先吃饭。

困。。。昨晚拓展方法-Event记录之后先休息一会。

填完/

完成了Animation部分的记录，似乎记录可以简洁一些，记录 最重要的 和 最需要扩展的 内容即可！

*******************
以上为19日日志
*******************

整理时发现巨大问题：
Controller体系实现太过失败：简单问题复杂化。
且有实体状态耦合，这里考虑将实体状态简化以达到目的。

今天的任务是再完成一个大类的文档总结，目前完成了UI，Animation，Event，Controller，今天早上的目标是Effect，Item，LaEmoji，有可能的话完成Entity的部分，
须知Entity本质上是几个组件拼接的类，再其余组件完成时其总结变得简单。

Effect是个小模块。现在开始大模块 LaEmoji，总结不完不吃饭。

LaEmoji的AnimationType是个作废属性。PageActive方法里使用它并无作用。
但是Handler可以添加正确的动作给M_LaEmoji包括背景（都很重要）所以，设计需要修改。

同时完成了修改和记录，吃午饭。

现在来完成Item类的记录。然后蹬三轮！

Item部分目前不考虑，因为感觉要大改或者干脆取消之。

有很多问题：1.实现函数的耦合。
2.访问方法的不确定性。
3.UseTime是一个多余设置。

非也，这些实现本可以非常优雅，但是时间过久你忘记了当出的想法。

1：UISet实际上已经实现为UIItem，
2：ItemState类控制的包括 UI显示和 Item数据 甚至包括使用函数
3：ItemEntity以以下形式存在，如在地上的可捡拾状态，和被投掷出去之后的飞行实体状态。
这里思考一个问题，为什么不把ItemEntity和Entity作相同的处理方式，ItemEntity继承自Entity，其实可以通过改动，让其回归本体，而且其实所有功能都可以实现在实体的有关属性中，这应该作为一个改动课题。但必须在实现物品体系和实体技能的可测试样本之后。
不如把它写入计划

仔细一想，DropFunc和UseFunc的确还有存在的必要，但是FlyEndFunc似乎没有必要？否，它可以实现结束飞行操作的多样性，而不限于32种实体物理类型。所以体系没有问题，但我花了很久时间来理解它。